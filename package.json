{
  "name": "github-table-enhancer",
  "version": "0.1.0",
  "description": "Makes wide tables easier to read in GitHub Markdown file previews.",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "node scripts/clean.mjs && tsc && node scripts/copy-static.mjs",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
